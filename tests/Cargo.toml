[package]
name = "tests"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
planetscale-driver = { path = ".." }
rand = "0.8.5"
tokio = { version = "1.27.0", features = ["full"] }
